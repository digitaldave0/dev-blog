{
  "name": "dev-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bundle exec jekyll build",
    "serve": "bundle exec jekyll serve",
    "regenerate-search": "./regenerate-search.sh"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
